#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>


int main()
{
	char * msg;
	
	while(1)
	{
		msg = (char *)malloc(100);
		memset(msg,0,100);
		strcpy(msg,"again\n");
		printf(msg);
		
		sleep(1);
		
		free(msg);
	}
}
